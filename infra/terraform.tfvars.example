# Copie para terraform.tfvars e preencha com valores reais.
# terraform.tfvars NAO deve ser commitado (ja esta no .gitignore).

aws_region       = "us-east-1"
instance_type    = "t3.micro"
ssh_allowed_cidr = "SEU_IP_PUBLICO/32" # descubra com: curl ifconfig.me
project_name     = "task-manager"
